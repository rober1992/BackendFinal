export interface Messages {
     userId : string;
    email : string;
    msg: string;
    timestamp: Date;
}

export interface NewMessages {
    userId : string;
    email : string;
    msg: string;
    timestamp: Date;
}